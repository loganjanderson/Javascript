// My solution

const toThePowerOf = (first, second) => {
    answer = first ** second;
    console.log(answer)
  }
  
  toThePowerOf(2, 3);
  
  
  //Jordan's Solution
  
  // const toThePowerOf = (num, exp) => {
  //   const items = Array(exp).fill(num);
  //   const reducer = (total, currentValue) => total * currentValue;
  //   console.log(items.reduce(reducer));
  // }
  
  // toThePowerOf(2, 3);
  
  